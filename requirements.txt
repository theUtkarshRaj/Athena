# Athena dependencies.
# cognee provides the memory layer; [gemini] pulls the Gemini LLM/embedding deps.
cognee[gemini]
streamlit>=1.36
python-dotenv>=1.0

# Optional: OCR for scanned / image-only PDFs. Kept OUT of the default install so the
# hosted (Streamlit Cloud) build stays light and reliable. The app's OCR code degrades
# gracefully when these are absent. To enable OCR locally, install:
#   pip install pymupdf>=1.24 pillow>=10 rapidocr-onnxruntime>=1.2
# (on Linux, rapidocr's opencv also needs apt packages: libgl1 libglib2.0-0)
