# Athena dependencies.
# cognee provides the memory layer; [gemini] pulls the Gemini LLM/embedding deps.
cognee[gemini]
streamlit>=1.36

# OCR for scanned / image-only PDFs (pip-only, no system binaries — deploys anywhere).
# PyMuPDF renders pages, rapidocr-onnxruntime recognises text (bundles its own models).
pymupdf>=1.24
pillow>=10
rapidocr-onnxruntime>=1.2
